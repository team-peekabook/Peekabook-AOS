<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="android.view.View" />

        <variable
            name="data"
            type="com.sopt.peekabookaos.data.entity.bookshelf.ShelfData" />
    </data>

    <FrameLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">

        <ImageView
            android:id="@+id/iv_item_bookshelf_shelf"
            setImage="@{data.book}"
            android:layout_width="96dp"
            android:layout_height="145dp"
            android:layout_marginTop="40dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:src="@color/peeka_beige" />

        <ImageView
            android:id="@+id/iv_item_bookshelf_shelf_pick"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="end"
            android:layout_marginTop="40dp"
            android:src="@drawable/ic_bookshelf_friend_plus"
            android:visibility="@{data.pick == true ? View.VISIBLE: View.INVISIBLE}" />
    </FrameLayout>
</layout>