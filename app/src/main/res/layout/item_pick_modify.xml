<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="android.view.View" />

        <variable
            name="data"
            type="com.sopt.peekabookaos.data.entity.Shelf" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">

        <ImageView
            android:id="@+id/iv_item_pick_modify"
            setImage="@{data.book}"
            android:layout_width="96dp"
            android:layout_height="145dp"
            android:layout_marginTop="40dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:src="@color/peeka_beige" />

        <ImageView
            android:id="@+id/iv_item_pick_modify_index"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintEnd_toEndOf="@id/iv_item_pick_modify"
            app:layout_constraintTop_toTopOf="@+id/iv_item_pick_modify"
            android:layout_marginTop="6dp"
            android:layout_marginEnd="6dp"
            android:src="@drawable/ic_bookshelf_friend_plus"
            android:visibility="@{data.pick == true ? View.VISIBLE: View.INVISIBLE}" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>